{% extends "_layout" %}

{% block content %}

	<div class="showcase-image">
		<picture>
			<img src="{{ entry.featureImage.first().url }}" alt="{{ entry.featureImage.first().title }}">
		</picture>
	</div>

{% endblock %}
